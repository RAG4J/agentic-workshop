spring:
  profiles:
    active:
    default: springai

  application:
    name: evals-web-app

  # Thymeleaf configuration
  thymeleaf:
    cache: false # Disable cache for development
    encoding: UTF-8
    prefix: classpath:/templates/
    suffix: .html

  # Jackson configuration for JSON handling
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: UTC

  # Web configuration
  web:
    resources:
      static-locations: classpath:/static/
      cache:
        period: 0 # Disable caching for development
  ai:
    openai:
      project-id: workshop
      api-key: ${openai.proxy.token}

openai:
  proxy:
    url: https://vfjv12h3xb.execute-api.eu-west-1.amazonaws.com
    token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyIjoic3R1ZGVudCIsImRlc2NyaXB0aW9uIjoid29ya3Nob3AiLCJpYXQiOjE3NTc0MTc5NjgsImV4cCI6MTc1NzQyODc2OH0.zrSUlVgK98sdPL294am_YKvpZV6fI307XAE5x-qTrGA


# Custom application properties
evals:
  data:
    directory: evals-web-app/src/main/data # Directory for storing evaluation data files
  questions:
    filename: input-questions.json # Filename for input questions (within data directory)

# Server configuration
server:
  port: 8081
  servlet:
    context-path: /

# Logging configuration
logging:
  level:
    org.rag4j.evals: INFO
    org.springframework.web: INFO
    org.thymeleaf: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Info endpoint configuration
info:
  app:
    name: Evals Manager
    description: Web application for managing AI evaluation records
    version: 1.0.0
